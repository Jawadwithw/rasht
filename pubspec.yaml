name: rasht
description: Rain City API tracing for Flutter — in-app Dio request inspector with cURL and Postman export.
version: 0.1.3
homepage: https://github.com/Jawadwithw/rasht
repository: https://github.com/Jawadwithw/rasht
issue_tracker: https://github.com/Jawadwithw/rasht/issues

environment:
  sdk: ^3.7.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.2
  font_awesome_flutter: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
